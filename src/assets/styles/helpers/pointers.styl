.application

    .cursor-default

        cursor: default

    .cursor-pointer

        // cursor: pointer!important
        cursor: pointer

    .cursor-not-allowed

        // cursor: not-allowed!important
        cursor: not-allowed

    .cursor-inherit

        // cursor: inherit!important
        cursor: inherit

    .cursor-move

        cursor: move
